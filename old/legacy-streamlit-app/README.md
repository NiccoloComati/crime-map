Archived Streamlit UI.

This folder preserves the retired Streamlit interface that previously rendered the Folium map directly from Python.

It is not part of the active application stack anymore. The current deployment path is:

- `api/` for the FastAPI backend
- `frontend/` for the Next.js web app

If you ever need to inspect the old UI, treat it as legacy reference code only.
